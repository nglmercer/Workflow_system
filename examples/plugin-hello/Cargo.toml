[package]
name = "workflow-plugin-hello"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-system = { path = "../../vendor/plugin-system-rs/crates/plugin-system" }
log = "0.4"
serde_json = "1"
